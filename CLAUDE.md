# zjmud-rust — 專案規則（Claude Code）

> 這一份只寫**閘門紀律**：什麼情況才可以說「好了」。
> 每一條後面都有它的來歷——全部是這個專案上真的踩過、而且是**使用者先發現**的。
> 架構與工具說明在 `README.md`、遷移流程在 `docs/zjmud_migration_SOP.md`。

## ★ 核心原則

**閘門的寬容度不可以高於產品。** 驗證管線只要比真實使用路徑寬鬆一格，
那一格就會變成使用者替你發現的 bug。這個專案的每一次「測試全綠但使用者一開就壞」
都是這條被違反。

---

## 1. 「完成」的定義：看**線上產物**，不是看 CI 綠燈

CI 綠 ≠ 使用者拿得到。部署與 CDN 有時間差，中間使用者測到的是舊版。

- ❌ 「CI success，已上線」
- ✅ `curl` 線上檔案，**確認新內容真的在裡面**才回報

```bash
# 例：確認某個修復真的上線了（而不是只看 workflow 狀態）
curl -s https://szupotseng.github.io/zjmud-rust/js/wasmdriver.js | grep -c clockOrigin
curl -s https://szupotseng.github.io/zjmud-rust/css/app.css     | grep -c 'App-shell'
```

【來歷】使用者兩次在部署完成前重測，看到的是舊版，回報「沒變」「還是怪怪的」——
浪費的是他的時間，而我當下說的是「已上線」。

---

## 2. 版面必須用**真的排版引擎**驗

**jsdom 沒有排版引擎**：`getBoundingClientRect()` 一律回 0，元素疊不疊、
點不點得到，它一律看不見。用 jsdom 說「全鏈路通過」＝只證明了邏輯，**沒證明任何像素**。

```bash
# 落出 in-world 當下的真 DOM，再用真 Chromium 在多視口量座標
ZJMUD_DUMP=/tmp/snap.html node tools/verify-fullstack.mjs --mud 91shujian --no-switch
node tools/check-layout.mjs /tmp/snap.html
```

- 判準是使用者原話的機械化版本：**重疊面積 > 400px²** 就紅；
  可點元素必須完整在視口內且 `elementFromPoint` 命中自己
- 視口一定要含 **in-app 瀏覽器（390×660）** 與 **最小實機（360×600）**——
  最嚴重的擠壓都在那裡發生，不在桌面
- 改任何 CSS／版面結構，這一格是**必跑**，不是選配

【來歷】使用者連續四次回報「介面互相覆蓋、根本點不到」，每一次我的閘門都是綠的。

> **快照必須自帶 `<base>`**：DOM 快照被寫到站台以外（CI 寫 `/tmp`）時，
> 相對路徑的 CSS 會整份載不到，元素照文流往下堆——CI 量到「送出鈕 top=1331、
> 視口高 900」判定被擠出視口，看起來像版面壞了，其實是快照不完整。
> `check-layout` 因此**先驗 CSS 有沒有生效**（`.layout` 的 display 必須是
> flex/grid），沒生效就明講「樣式沒載到」而不是報成版面問題。
>
> **讀不到 CI 日誌時，讓 CI 自己把診斷發佈出來**（`site/_layout-report.txt`）。
> 在黑箱裡猜第三次以上就該改成取得資料——這一格的真因就是這樣查出來的。

---

## 3. 測試資料要挑**最容易踩雷的合法輸入**，不是最容易過的

- 長度限制 → 取**下限**（不是中間值）
- 字集 → **簡體與繁體各跑一次**
- 帳號 → 用各台交集都合法的最保守形式

【來歷】建角測試用「令狐一郎」（四個字），17 台全過；使用者一打兩個字就 15 台被擋——
那些 mudlib 的長度檢查數的是 GBK 位元組，**四個字剛好是唯一過得了的長度**。
一筆巧合的測試資料把必然的 bug 藏了幾天。現在有 `tools/sweep-names.mjs`（長度 × 字集全跑）。

---

## 4. 驗證管線的「體貼」要同步回產品

驗證腳本為了跑得順而加的退路，**產品端也必須有**，否則那條退路就是在掩蓋缺陷。

【來歷】`boot-test` 早就有「等不到版本挑戰就照樣送帳號」的退路，所以 node 端一直全綠；
而客戶端沒有這條退路——大梦江湖／nt7 在真網頁路徑上連上之後畫面全白，
缺口就藏在「17 台都 playable」底下。

---

## 5. 覆蓋率要沿著**使用者實際走的路**展開

只驗一台、只驗到登入畫面，就只能宣稱那一台、那一段。

- `tools/sweep-web.mjs`：**每一台**都走完整網頁路徑（每台一個子行程做隔離）
- 斷言要是**實數**，不能是套套邏輯：
  - ❌ 「快捷列有 17 個槽位」← 空槽也算，永遠成立
  - ✅ 「有內容的槽 > 0」「訊息區比登入前長 200 字以上」

【來歷】第一版 sweep 只驗一台，其餘 16 台只被沒有 DOM 的 node 測試驗過；
而「17 台都 playable」這句話當時只有一台被真的用瀏覽器證明過。

---

## 6. 修正工具必須**冪等，包含「改變主意」**

「看到自己的標記就跳過」會讓改良版永遠補不進去，而報告還顯示「已處理」。
正確作法：**先撕掉自己上一次補的區塊，再重補**。

【來歷】`fix-image.mjs` 為此踩過三次（simul_efun shim、valid_read 白名單、is_chinese）。
`is_chinese` 的門檻是白名單式的，同一條規則**連漏三次**——漏改比不改更難發現。

---

## 7. 症狀相同不代表真因相同——同一個 bug 修第三次要換層次思考

【來歷】driver 時鐘錯了三次：`Date.now()`（epoch）→ `performance.now()`（分頁載入起算）
→ 正解是**開機當下記原點、之後送差值**。前兩次都只是「換一個看起來比較對的時間來源」，
沒問「driver 到底要的是什麼」——它要的是**經過了多久**，不是現在幾點。

---

## 8. 每個結論都要能指到證據

規則／參數／欄位限制一律**從原始碼或實測讀出來**，不要猜。猜錯的代價是下一層症狀更難懂。

- 埠號 → 從 `master::connect()` 讀（nt7 的 zjmud 模式綁在 `port == 333`）
- `valid_read` 的 `func` 名稱 → 插一行 `debug_message` 印出來（猜 `include_file` 是錯的，真的是 `include`）
- 欄位限制 → 從該 lib 的 `logind` 讀進 `FIELD_SPECS`，**在 client 送出前就擋**

---

## 9. 補件與嫁接：要能**量化證明是同一個東西**

封存缺件可以從別處補，但必須是**同一款遊戲的另一份快照**，而且判準要能算：

- 比對 `kungfu/` 檔名集合（武功目錄最具指紋性）——nt7 對 nitan170911 是 **99.7%**
- 規則只有一條：**目標自己有的檔一律不覆蓋**
- 補了什麼**逐項記進 `mud.json` 的 `graft` 欄位**
- 補件同樣守隱私：`data/**/*.o`（含明文密碼的玩家存檔）永遠不收

---

## 10. 誠實分級：假綠燈比紅燈危險

跑不完就是 `limited`，不要用寬鬆的判準把它算成 `playable`。

【來歷】nt7 曾被判成 playable——實際上 zjmud 分支送的 `look` 被伺服器當成英文名字，
opcode 數量剛好過門檻。降級並把真因寫進 `mud.json` 之後，才停止誤導。

---

## 提交前必跑

```bash
cd webclient
npm test                                   # 單元＋整合
node tools/build-site.mjs --out ../site    # 每台真的開機測試
node tools/sweep-web.mjs                   # 每台走完整網頁路徑
node tools/sweep-names.mjs                 # 建角：長度下限 × 簡繁
ZJMUD_DUMP=/tmp/snap.html node tools/verify-fullstack.mjs --mud 91shujian --no-switch
node tools/check-layout.mjs /tmp/snap.html # 版面：真 Chromium × 四視口
node tools/verify-clicks.mjs --mud <slug>  # 選單：每顆按鈕真的按下去，看世界有沒有動
cd .. && bash scripts/privacy-scan.sh && node scripts/check-badges.mjs
```

改動只碰前端 UI 時，至少要跑：`npm test` ＋ `verify-fullstack` ＋ **`check-layout`**。
改動碰到 **daemon 產生器（`convert-to-zjmud.mjs`）或協議解析（`protocol.js`／`ansi.js`）**
時，必須加跑 **`verify-clicks`**——那是唯一會真的把按鈕按下去的一格。

## Git

- `git diff -w` 先看真實改動（CRLF 會讓整檔假性變動）
- 只 `git add` 明確檔案，**禁止 `git add -A`**
- WSL 推不動時用 Windows `git.exe`（憑證管理員在 Windows 側）

## 11. 「能不能玩」要用**行為**驗，不能用**訊號**驗

閘門檢查「有沒有收到 opcode」時，證明的是**轉換層會翻譯**，
不是**世界可玩**。兩台實測都在這個縫隙裡拿到假綠燈：

- 泥潭170911：面板齊全，實際卡在「泥潭注册室」，房間沒有出口
- 北美2001：面板齊全，實際卡在腳本開場，`block_cmd` 擋掉所有移動

**規則**：可玩的判準必須是**做得到那件事**——實際送出方向、確認房間變了。
凡是「收到某個訊號就算成功」的判準，都要先問一句：
「訊號出現但事情沒發生，會長什麼樣子？」答得出來就代表判準不夠。

## 12. 同一性判斷要用**指紋**，不要用**單一欄位**

北美的海岸線上連續好幾間房間**全叫「沙滩」**。用房間標題判斷有沒有移動，
會把一台完全正常的台判成故障。改用「標題＋出口清單＋房內物件」的組合指紋。

**規則**：拿某個欄位當「身分」之前，先確認它在真實資料裡**真的唯一**。
中文 MUD 的房名大量重複（沙滩／小路／山道／甬道往往連成一串），
這不是特例而是常態。

## 13. 掃原始碼的修正，副檔名一律 `.(c|h|lpc)`

`fixLogDirs` 原本只掃 `.c|.h`，而 mudlibs-main 的 telnet lib
**整棵樹都是 `.lpc`**——這條修正對它們一直是**靜默失效**的：
不報錯、不警告，報告上看起來一切正常。

**規則**：任何依副檔名過濾的程式碼，都要對照**實際資料**確認涵蓋範圍；
新增掃描類修正時一律寫 `.(c|h|lpc)`。與 §6（覆寫而非跳過）同源——
**靜默失效比明顯失敗危險**。

## 14. 「有這個名字」不等於「有這個定義」

`detect_look()` 用 `look_room\s*\(` 判定某個檔案「有 look_room」，
結果抓到的是**呼叫**：

```lpc
msg += (env->query_module_file())->look_room(env) || "";
```

於是 builder 認定可以注入，實際上那個檔案裡根本沒有 look_room 的**本體**。

**規則**：偵測函數定義時，正則一律要求 `)\s*{` 結尾。
這條 SOP §C3 早就寫過（前向宣告 vs 定義），但新寫的偵測函數沒有套用——
**同一條紀律要在每個新增的偵測點重新確認一次，不能假設自己記得**。

## 15. 修閘門的槓桿比修個案大

本 session 反覆出現同一個形狀：症狀指向某台 mudlib，真因在驗證管線這一側。

| 表面症狀 | 真因 |
|---|---|
| 「這台 ID 不合法」 | 閘門的測試帳號踩到該 lib 的禁用字 |
| 「這台登入成功但畫面空白」 | 閘門只等 3 秒，而自己排的 look 排到 9 秒 |
| 「這台的模板改了沒生效」 | 映像載入偏好 `.gz`，而 JS 工具只寫 `.data` |
| 「0 台，全部通過」 | 空集合的套套邏輯斷言 |

修一條閘門缺陷往往一次拉起十幾台；修一個個案只拉起一台，
而且下一台還會再撞一次。**先問「是被測的東西壞了，還是尺規壞了」。**

## 16. 批次操作前先確認「這個東西該不該被動」

批次轉換 23 台 telnet lib 時沒有檢查它們是不是**本來就會說 zjmud**，
把原生的 nt7 從「可連線」弄成「開不了機」。

**規則**：批次操作最危險的不是做錯，而是**對不該動的東西動手**——
而那通常在報告上看起來一切正常（每一台都顯示 ✔ 轉換成功）。
先寫出「什麼情況要跳過」，再開始跑。

## 17. 自己寫的閘門越完整，越容易忘記別人寫的閘門

我為 builder 建了一整套專屬驗證（開機測試、行為驗證、真瀏覽器逐台掃描），
每一輪都跑得很勤——卻讓專案原有的 **207 個單元測試靜置了幾十輪**。

壞掉的正是其中一個記錄了血淚教訓的測項：

```
✖ ★ 同一行有兩個提示時，答後面那個（星戰英雄實錄）
   expected: ['y']   actual: ['wasmtest']
```

真因：我為排除 BIG5 而在 confirm 規則前加了 `[\s\S]*?`，
那讓匹配從**位置 0** 開始，而排序判準是「命中位置越後面越優先」——
confirm 因此從第一名掉到最後，會回去答已經過去的那一題。

**規則**：新增的驗證是**加法**不是取代。
每一輪至少要跑一次 `npm test`，尤其在改動 `src/js/` 之下的任何檔案時。
既有測試裡的每一個 ★ 測項都是某次故障的墓碑，改壞它等於把那次故障放回來。

## 18. 同一個症狀修第三次，就該做成不變式檢查

「映像位元組錯位」在本 session 出現三次，我每次都從 git 還原那一台、
繼續往下做。三次都沒問「為什麼老是它」。

真因是 `.gz` 沒跟著 `.data` 更新（載入端優先讀 `.gz`），
而症狀是「`include/globals.h` 語法錯誤」——完全指向錯的地方。

**規則**：個案還原不會累積成防線。同一個症狀第三次出現時，
停下來把它變成**可重複執行的檢查**（`mud2zjmud doctor`），
而不是再還原一次。檢查寫好後第一次跑就抓到一台，
那一台我先前還為它做過錯誤的診斷。

## 19. 提交前必跑（更新）

```bash
cd tools && python3 -m mud2zjmud doctor     # ★ 映像不變式（新增）
cd ../webclient && npm test                 # 單元＋整合
node tools/build-site.mjs --out ../site --skip-boot-test
node tools/sweep-web.mjs                    # 每台走完整網頁路徑
node tools/sweep-names.mjs --limit 20       # 建角：長度下限 × 簡繁（抽驗）
node tools/check-catalogue.mjs ../site  # ★ 目錄頁搜尋/篩選（真 Chromium 打字點擊）
node tools/verify-clicks.mjs --mud <slug>   # ★ 選單：每顆按鈕真的按下去
cd .. && bash scripts/privacy-scan.sh && node scripts/check-badges.mjs
```

**部署後一定要 `curl` 線上產物確認**（§1）——CI 綠燈不等於使用者拿得到。

## 20. 加新旗標時，回頭看自己寫過的警告

`sweep-web` 的 `VALUE_FLAGS` 上方有一整段註解，講 `--only` 的值被當成
positional 參數（站台路徑）的那次事故。我後來加 `--limit` 時**沒有把它登記進去**，
於是 `--limit 12` 的 `12` 又被當成站台路徑，程式跑去找
`webclient/12/libs/index.json`——**同一個坑，同一個檔案，同一段註解正上方**。

**規則**：修改一張「登記表」性質的資料結構時，先讀它的註解。
註解就寫在那裡，是為了讓下一個人（包括三小時後的自己）不要重犯。

## 21. 「有沒有收到訊號」與「按下去有沒有用」是兩件事

使用者：「play.html?mud=yanhuangwuhun，另外請測試所有選單，我登入後測試完全沒反應」。
當時**每一個閘門都是綠的**：opcode 四項全中、面板長出來、房間能看能走。

真因是我把 `$zj#` 當成**欄位**分隔符，而它分隔的是**記錄**。
一個誤解同時連錯三個 opcode：`006` 快捷列每一顆的指令都是空字串（按了不送任何東西）、
`021` 標題列的標籤與指令各自變成一顆按鈕、`008` 動作列同樣錯位。
`003`／`005` 我剛好寫對了，所以出口與「現場」看起來完全正常——
畫面愈像沒事，愈難相信是協議層錯了。

**規則**：任何「產生給客戶端解析的字串」的程式碼，判準必須是
**讓客戶端真的解析一次，再把結果按下去**。
現在有 `tools/verify-clicks.mjs`：真 Chromium 逐顆按，三件事都要成立——
① `elementFromPoint` 打得到自己 ② 送出紀錄裡真的出現那個指令
③ 世界有回應且不是「什麼？」。

同一輪還修掉三個**閘門自己的**缺陷（假紅燈與假綠燈一樣危險）：

| 閘門缺陷 | 後果 |
|---|---|
| 指令從 DOM 猜（`data-cmd`），但真相在 store 的閉包裡 | 四顆正常的按鈕被報成「沒掛指令」 |
| 用固定睡眠等非同步送出／回應 | 慢一拍就報紅——變成在量測機器速度 |
| 「刻意跳過」與「靜默失效」共用同一條路徑 | 7 台**原生 zjmud**被報成失敗 |

## 22. 規格文件寫錯，實作就會錯得「有據可查」

`docs/zjmud-spec.MD` §3 的表格自己就把 `006` 記成 `槽位:標籤$zj#指令$zj#…`。
我實作時照著它寫，還覺得有依據。**文件也是閘門的一部分**——
它錯的時候，錯誤會沿著「有出處」的路徑傳播，比沒有文件更難發現。

改協議相關的實作時，順手回頭驗一次規格表：拿客戶端的 parser
（`protocol.js` 的 `parseButtons`／`parseTitleButtons`／`parseActions`）
對照表格逐欄看一遍，比相信自己上次寫的快。

## 23. 「沒有輸出」不是證據——先證明你的探針會出聲

追西游记451 時，我連續三輪把探針的問題誤讀成被查對象的問題：

| 我看到的 | 我下的結論 | 真相 |
|---|---|---|
| `--debug` 一行都沒印 | 「中斷點在 ZJ_INIT_NEW 之後」 | `#define ZJ_DEBUG` **根本不在映像裡**（後續批次重生沒帶 `--debug`，把它蓋掉了） |
| 新探針兩行都沒印 | 「中斷點比原先更早」 | 探針用 `tell_object(登入殼)`，那不是有效輸出目標——**在完全正常的台上也不會印** |
| catch 之後的探針沒印 | 「執行緒死在 setup() 裡」 | `setup()` 回來的瞬間輸出是黑的（`this_player()` 不隨 `exec()` 改變）。改用狀態追蹤後：`init 回顧 = ok`，**根本正常完成** |

**規則三條**：

1. 查之前先確認探針**真的在產物裡**（`grep '#define ZJ_DEBUG' <生成檔>`）。
   批次工具不帶 `--debug` 會靜靜把它蓋掉。
2. 探針要先在**已知正常**的目標上跑一次，判準是「**會不會出聲**」，
   不是「會不會編譯」。spec §D6 原本只寫到編譯那一層，不夠。
3. 「這一行有沒有跑到」要用**狀態**回答，不要用輸出——
   狀態跨執行框存活，輸出不會。寫進物件欄位，下一個框再回讀。

推論鏈上任何一環是「我沒看到 X」，都要先問：**「X 出現的話，我看得到嗎？」**

## 24. 原生化要吸收的不只是對話，還有**身分**

`registered` 這個欄位決定指令搜尋路徑；沒設的話 `/cmds/skill/` **整個目錄**
從玩家眼前消失，而 `hp`／`look` 一切正常——看起來像「這台沒有那幾個指令」。

而且**只在建角時設是不夠的**：`enter_world()` 會重讀 dbase 把它洗掉，
`enable_player()` 再用洗掉的值重算一次路徑。要在進世界之後再設一次，
並且呼叫 `enable_player()` 讓它重算——**光設值不會生效**。

**規則**：telnet 的註冊流程設過的每一個欄位，都是原生化的待辦事項。
判斷「補了沒生效」時，要問的不是「值有沒有設進去」，
而是「**讀這個值的那段程式碼，在我設完之後還會不會再跑一次**」。

同源的還有 `born`（§D23）。兩者都符合：檔案在、沒有編譯錯誤、
其他指令正常，只有某一塊世界對玩家關著門。

## 25. 「沒有錯誤」也可能是壞掉——分頁器在等一個不會來的按鍵

西游记451 卡了四輪。協議流停在 `0007`，沒有崩潰、沒有例外、沒有編譯錯誤。
真因是 `enter_world()` 裡的 `start_more(MOTD)`：**分頁器接管了輸入，等玩家按 q**。
上游的手動記錄裡就寫著那一步（`q (skip paginated MOTD)`）——真人會按，我們不會。

**規則**：追「流程停住」時，除了問「哪裡出錯了」，還要問
「**它是不是在等什麼**」。等待不會留下任何痕跡，所有找錯誤的方法都不會有收穫。
telnet mudlib 裡的分頁器、`input_to` 確認、`more` 提示，對 zjmud 客戶端都是死路。

修法要**保留內容**：把 `start_more(X)` 換成 `tell_object(user, X || "")`，
不是把 MOTD 清空。清空能讓守衛跳過（也有效），但那是把公告丟掉——
使用者本來就該看到它。能不損失資訊就不要損失。

## 26. 部署方式換了，工具的假設要跟著換

倉庫轉 private 之後 GitHub Pages 不可用，站台改由 Railway 供應。
Pages 是「吃一個目錄」的模型，所以 `build-site` 一直把 1.4 GB 的映像
**複製**進 `site/`；Railway 是「跑一個程序」的模型，而那個程序讀得到 `libs/`
——複製就只是讓容器多背一份、每次部署多搬一次。
加了 `--link-libs`（大檔留在原處，伺服器回退供應）之後產物 1.4 GB → 189 MB。

**規則**：換平台時，先問「舊平台的哪些限制是我為它做的妥協」。
那些妥協在新平台上多半變成純粹的成本，而它們不會自己消失——
因為程式碼看起來一直都在正常運作。

## 27. 症狀出現的地方，和真因所在的層，可以差很遠

`hy`：玩家下任何指令都得到「什麼？」，看起來像「這台沒有這些指令」。
真因是**五層之外**的一個權限檢查——`valid_read` 擋住 mudlib 讀自己的天氣表，
daemon 因此建不起來，`look` 依賴它而失敗，`call_other` 回 0，才落到那句話。

**規則**：`call_other(…) 回 0` 這一類「找得到但沒生效」的問題，
第一步是去讀 **driver 的執行期錯誤**（`bootMud` 的 `log` 陣列——
`boot-test` 的 rawLog 拿不到）。那裡有完整的呼叫堆疊，
而症狀本身完全指不到真因。

## 28. 白名單是打地鼠，先問「這個檢查要保護的是什麼」

放行 `valid_read` 時我第一版寫成白名單（`/adm/etc`），
結果同一台的另一個 daemon 讀 `/quest/` 又中同一個坑。逐個目錄補永遠補不完，
而且每漏一個都長成完全不同的症狀。

反過來問「**要保護的是什麼**」——玩家存檔與巫師家目錄——就得到
「除了那兩處，其餘放行」，範圍明確、不會再漏，而且要保護的東西一個字沒動。

**規則**：要放寬一個檢查時，先寫出它**存在的理由**，
再從理由推出範圍。從症狀往回列舉例外，列不完。

## 29. 「缺件」這個結論要先證明，不要從錯誤訊息直接抄

`lpmudname`（本專案同名的上游 江湖論劍）在收藏裡標著 noboot，
理由寫「開機時拋例外：ENOENT … mudlib.json」——看起來就是映像缺件。

實際上那台**刻意不放映像**（`mud.json` 的 `work` 指向倉庫裡的
`LPMud-Name/world`，README 明講「不複製一份」）。ENOENT 的真因是我加的
`--link-libs` 讓 boot-test 對**非 prepacked** 的台也去讀 `lib.dir`。

**規則**：ENOENT 只說明「那個位置沒有檔案」，不說明「這個東西不存在」。
下結論之前先問：**它本來就該在那裡嗎**？
本專案的每一台都有 `mud.json` 記錄它的來源與 `work`，五秒就能查證。

## 30. 改變「東西放在哪裡」的旗標，會踩到所有假設路徑的程式碼

`--link-libs`（映像留在 `libs/` 不複製進 `site/`）已經連續踩到兩處：
`wasm.test.mjs` 直接讀 `site/libs/<slug>` 去開機（§D30）、
`build-site` 對非 prepacked 的台傳錯 image 路徑（§D31）。

**規則**：加這類旗標時，**主動盤點誰在假設路徑**——
`grep` 一次那個目錄名，把每個讀它的地方都看過，
而不是等它們一個一個壞掉。壞掉時的症狀（ENOENT、noboot）
通常指向完全錯誤的方向。

## 31. 判準在邏輯上更對，不等於改了會更好

`protocol-panels` 用「logind 有沒有 `ver1.0` 握手」決定要不要注入面板——
那確實是把「說得出 zjmud 登入」當成「畫得出 zjmud 畫面」，邏輯上站不住。
我把它改成量真正的東西（`cmds/` 裡用到面板巨集的檔數：真原生台 3 個，
nt7 是 0 個），判準乾淨多了。

**然後 nt7 從 limited 掉成 noboot。**

原生 zjmud 的殼再塞一份 `zjmud.h ＋ zjmudd ＋ look hook` 會與既有的打架，
而後果不是「面板沒出來」，是整台開不了機——這條 spec §D8 早就寫過。
已還原並撤銷判準改動。

**規則**：改判準之前先想清楚「**改對之後要做什麼**」。
如果後續動作本身有風險，那麼判準更精確只是讓風險發生得更頻繁。
負面結果要寫進文件（§D32），否則下一輪的自己會再走一次同一條路。

## 32. `catch()` 吞掉的不只是錯誤，還有你需要的線索

追「某個東西沒發生」時，`nitan7` 的整條鏈藏在一個 `catch` 後面：

```lpc
if( !catch(load_object(startroom)) )
        user->move(startroom);     // catch 有值 → 這行被跳過，而且悄無聲息
```

driver log 是**乾淨的**（0 個編譯錯誤），因為那個檔不在預載清單裡，
唯一會觸發它的那次載入被 catch 包著。

**規則**：追「沒發生」的事情時，先在路徑上找 `catch(` ——
把它的回傳值撈出來（存進欄位、下一個執行框印），常常一步到位。
本專案已經被 catch 咬過兩次：§D21 是「接不住物件消失」，
§D32-c 是「接得住，但你看不到內容」。

## 33. 改寫是對的，一次改 156 個檔就是錯的

`nitan7` 的真因確定無疑：`inherit/room/room.c:342` 的
`varargs void delete(...) { … return ::delete(idx, ob); }`——
void 函數回傳值，FluffOS 拒絕編譯，整個起始房間因此載不起來。

寫了通用規則、單元檢查正確、命中 156 個檔（含那個關鍵檔）——
**然後 nitan7 退化了**（從「收指令、有 opcode 000」變成「停在 authing」）。

**規則**：修正的**影響面**與它的**正確性**是兩件事。
一個語意正確的改寫，套在 156 個檔上仍然可能在某處產生副作用，
而你不會知道是哪一處。做法是：先只改**擋住路的那一個檔**，
量一次；真的需要更廣時一次加一個。

這與 §31（判準更對 ≠ 改了更好）同源，差別在這次連「改對」都成立，
壞在一次改太多。**最小步幅不是慢，是唯一能歸因的走法。**

## 34. 範圍該由「症狀」決定，不是由「樣式」決定

`nitan7` 的 `void` 函數回傳值：用樣式掃整棵樹 → 命中 156 個檔 → **退化**；
只改**擋住起始房間那一條繼承鏈上的一個檔** → **playable**。

同一個改寫、同樣正確，範圍不同結果相反。

**規則**：找到一個可以泛化的缺陷樣式時，先問「**症狀是從哪一個檔來的**」，
只改那一個，量一次。樣式告訴你「哪裡還有同樣的寫法」，
但那不等於「哪裡需要改」——沒有造成症狀的地方，改它只有風險沒有收益。

## 35. 同一台連續三輪沒有進展時，換成廣度驗證

`nt7` 我連追三輪：確立了它的混血形狀（§D33）、找到 zjmud 分支缺 `return`
讓兩條登入路徑互相踩踏（§D33-b）、也證實補上 `return` 會改變流程——
但 badge 一次都沒有動，而每一輪的修法都因為「沒有帶來改善」而撤回。

第三輪結束時我停下來問：**這一輪還能產出什麼**？
答案是「更多關於 nt7 的知識」，而不是「收藏更好用」。

於是改跑**廣度回歸**：`sweep-clicks --limit 20`，真瀏覽器逐台按每一顆按鈕。
結果 **20/20 全部通過**——這個 session 動了幾十處規則與映像，
而使用者實際體驗零退化。那個結論比 nt7 的第四份筆記有價值得多。

**規則**：單一目標連續三輪沒有推進 badge 時，先把時間換成**廣度**——
證明既有成果沒有壞掉，往往比再挖一層更值得。
知識可以留在文件裡等下一次（§D33-b 已經寫好下一步該查什麼）。

## 36. `git checkout` 還原不了 gitignore 的檔——而那正是載入端優先讀的那一份

`mudlib.data` 是 gitignore 的，只有 `.gz` 進版控。於是
`git checkout -- libs/<slug>` **只還原 `.gz`**，`.data` 留在舊版——
而載入端優先讀 `.data`，映像因此錯位。

症狀極度誤導：某個檔的**開頭被吃掉**。實測 `chongshengdeshijie` 的
`simul_efun.lpc` 第一行從 `/* This program is a part of RW mudlib` 變成 `udlib`
——看起來像那個檔壞了、或被哪條規則吃掉，實際上檔案好好的，
只是讀取的視窗偏移了。我差點據此宣稱「我們的映像損壞了那個檔」。

本 session 我手動補救了**三次**（nt7／nitan7／chongshengdeshijie）。
第三次就該做成工具（§18）——現在有 `doctor --fix`：
發現問題時先從 `.gz`（版控的真相）重建 `.data` 再驗一次，並說明它做了什麼。

**規則**：任何「還原」動作之後，先問**哪些檔不在版控裡**。
它們不會被還原，而如果其中有「載入端優先讀」的那一份，
你還原的是一個**不一致的組合**——比沒還原更難查。

## 37. 「影響面」要用數字說，不要用感覺說

上一輪我因為「156 個檔太多」撤回一個正確的改寫（§33），這一輪又做了一個
看起來類似的修正（`ZJ_ENTER` 巨集守衛）。差別在哪？**我量了**：

```
會變成 no-op 的台: 1 ['chongshengdeshijie']
不受影響的台: 208
```

因為那個分支只在「該 lib 沒有 `enter_world`」時才走到，
208 台**在結構上不可能受影響**——不是「我覺得應該沒事」，是條件本身排除了它們。

**規則**：決定一個修正該不該留下時，先把影響面**算出來**。
「只在 X 條件下才生效，而符合 X 的只有 N 台」是可以驗證的陳述；
「應該影響不大」不是。前者可以留，後者要先變成前者。

## 38. 二分法的價值在於「證明前提錯了」，不只在於找到答案

我為 `chongshengdeshijie` 寫下的下一步是「二分法找出哪個 `#include`
讓 master 建構中斷」。做了之後：**23 個全保留** FAIL、**23 個全註解** 也 FAIL。

兩端同時 FAIL ＝ 那個變數根本不是原因，**問題出在我的前提**。
再查一步就清楚了：FluffOS 的 master object 由 driver 直接建構，
不走一般的 `create()` apply——「`create()` 沒印」從頭到尾就不是異常。

**規則**：二分法跑之前先確認**兩端會給出不同結果**。
如果全開與全關都一樣，你量的不是那個變數——這時候最有價值的產出
不是答案，而是「把一整條線索劃掉」。**把排除掉的假設寫進文件**，
下一輪的自己才不會再走一次（這一台我已經排除五條）。

## 39. 「缺口太整齊」是缺陷的指紋，不是資料的樣子

208 台裡 022 缺 48 台、005 缺 26 台——都是資料相依（沒有生物、沒有物件），
這我一直知道。但 xkx2017 缺的是「**004 之後的全部**」，
而那五個 opcode 分屬五個不同來源，資料不可能剛好同時讓它們全部沒有內容。

真因是一個物件的 `short()` 丟例外，把整個 `look` 指令中止了。

**規則**：分辨「資料相依」與「缺陷」，看**缺口的形狀**：
散落的缺口通常是資料；**連續的尾巴**（某一格之後全沒）幾乎一定是
有東西中止了執行。這也是為什麼九個面板不該串在同一個函數裡——
把它們串起來，等於讓任何一格的失敗連坐後面全部。

## 40. 三個變體各只改一處，才知道是哪一處在作用

第一版修正我一次改了三件事（型別判斷、拿掉硬轉、加 catch），結果 9/9。
如果就此收工，我會把「加型別判斷」當成修法寫進文件——**而它其實無效**。
拆成三個變體各只改一處之後才看清楚：只有 `catch` 那一項有用。

**規則**：修好了之後還要再問一次「是哪一處修好的」。
一次改三處然後成功，你學到的是「這三處的某個組合有用」，
那個結論下次套不上去。與 §38 同源——**兩端要能給出不同結果**，
變體要能互相區分。

## 41. 索引不可以比站台樂觀

`site/libs/index.json` 宣稱 `linkedLibs: false`（站台自足）並列出 214 台，
而 `site/libs/` 底下**只有 9 台真的有映像**——`--only` 建置合併索引留下的。

本機完全看不出來：`serve-site` 有回退，缺的會從倉庫 `libs/` 補上。
**但 GitHub Pages 沒有那條回退**，其餘 205 台部署上去就是 404。
而先壞掉的是 `wasm.test.mjs` 的 ENOENT，看起來像測試環境問題。

**規則**：任何「清單」與「它指向的實體」之間都要有數量不變式，
而且要在**產生清單的地方**檢查（`build-site` 現在會擋）。
局部建置警告、全量建置直接失敗——因為全量建置的產物是會部署出去的。

## 42. 「有這個名字」不等於「有這個東西」——空目錄會冒充已完成的工作

收錄清單是用目錄名算的：`comm -13 <(ls libs) <(ls 上游/libs)`。
`libs/hellxg/` 是個**完全空的目錄**（連 `mud.json` 都沒有），
於是那台被當成已收錄，清單顯示「見底」——而它從來沒被轉過。

查下去它上游自己標 noboot、確實不該收。但**那是查了才知道的**；
空目錄讓人連查都不會去查，而且它會一直看起來像已完成的工作。

**規則**：任何用「名字存在」當完成判準的地方，都要再問一句
「**內容也在嗎**」。這條與 §14（有名字 ≠ 有定義）同形，
差別在那次是函數、這次是目錄——所以它不是特例，是一個會反覆出現的形狀。
已做成 `doctor` 的第六個不變式：`libs/` 底下每個目錄都要有 `mud.json`。

## 43. 閘門要求的東西，產品在正常路徑上必須真的會產生

我上一輪加了一條斷言：「每台都要有建置時的開機紀錄（`boot-test.json`）」。
它抓不到任何真問題，卻會**必然失敗**——那個檔只有帶開機測試的建置才會寫，
而標準建置（含 CI）用的是 `--skip-boot-test`。

**規則**：寫斷言之前先問「產品在**正常**路徑上會不會產生這個東西」。
會，才可以拿它當判準；不會，那是在驗一條產品沒有走的路。
改驗隨時都成立的那一條就好：索引的 badge 與各台 `mud.json` 記的必須相等。

## 44. 同一台在兩台機器上結論相反，先懷疑尺規綁在時鐘上

`lpmudname` 本機 playable 9/9、線上 limited「只收到 000 006 021」。
不是這台不穩——是 `boot-test` 進世界後的 `look` 固定送四拍
（500/2500/5000/9000ms），慢的機器上那四拍全落在世界就緒之前。

**規則**：閘門裡每一個固定秒數都是一個「這台機器有多快」的假設。
判準要綁在**事實**上（002 出現了沒），重試要**敲到門開為止**。
改法要挑影響單向的那一種：多敲幾次只可能讓慢的台變好，不可能讓好的台變壞——
**單向的改動才可以在沒辦法逐台重測時放心留下**（§37 的量化同源）。

## 45. 兩個探針結論相反時，那個矛盾就是答案

`status_panel` 我放了兩個探針：函數**開頭**印得出來，函數**結尾**印不出來。
單看任一個都會下錯結論（「沒被呼叫」／「被呼叫了」），
兩個放在一起才指出真相：**它在中間中止了**。

**規則**：追「某段程式碼有沒有跑到」時，探針要放**兩個**——
入口一個、出口一個。一個探針只能回答「到過這裡嗎」，
兩個才能回答「跑完了嗎」。而「跑到一半炸掉」正是最難從症狀看出來的那一種，
因為它和「條件不成立所以沒做事」在外面看起來一模一樣。

## 46. 「沒有輸出」第三次騙到我——這次是 cwd 被重置

我判定「`status_panel` 從來沒被呼叫」，依據是探針一行都沒印。
真相是 shell 的 cwd 被重置，`tools/boot-test.mjs` 指到了倉庫根目錄的
`tools/`（那裡沒有這支），錯誤訊息又被我自己的 `grep` 濾掉了。

spec §23 寫的是「先證明你的探針會出聲」，我當時想的是探針本身；
這次壞的是**跑探針的那條指令**。

**規則**：`... 2>&1 | grep <pattern>` 會把失敗訊息一起濾掉——
探針沒出聲時，第一步是**把 grep 拿掉看完整輸出**，不是開始推論。

## 47. 「已知不用碰」的項目，也要有一次實測把它變成事實

`005`／`022` 的缺口被標成「資料相依、不要碰」很久了，而那是**推論**：
新手房沒有物件、裝飾 NPC 沒有生命值。推論是對的——但在 §D35
（一個壞物件吃掉五個 opcode）之後，同樣的缺口也可能是**中止**造成的，
兩者從外面看完全一樣。

量一次的成本很低：把保護加上去重建三台（沒有變化 → 不是中止），
再用探針讀事實（起始房只有玩家一個物件；NPC 只有 chat_chance/age/level）。

**規則**：文件裡每一條「這個不用查」都應該有一次實測背書。
沒有背書的「已知」會在假設失效之後**繼續看起來正確**，
而它擋掉的正是重新檢查的動作。

## 48. 追不到真因時，先問「這台有沒有把某個 efun 換掉」

`chongshengdeshijie` 追了四輪都在猜「它哪裡壞了」。真相是它**沒有壞**——
它把 `write`/`tell_object`/`say`/`tell_room`/`message` 全部改成
「呼叫就報錯」的樁（原作者要強迫大家改用新 API），
而**我們注入的程式碼整段都靠那些 efun**。

也就是說：不相容的是我們，不是它。

**規則**：注入式的轉換工具，預設假設是「基礎 efun 可以用」。
那個假設在改寫過 simul_efun 的 lib 上不成立，而失敗會出現在
離注入點很遠的地方（這次是 `fluffos_connect 失敗`，隔了五層）。
症狀查不下去時，去 `simul_efun/` 看一眼我們用到的東西有沒有被換掉——
成本一分鐘，而它擋掉的是好幾輪的瞎猜。

## 49. `catch()` 連續吃掉三層，就是三層的線索

這一台的鏈上有**三個** `catch`：`connect()` 一個、`logon()` 一個、
`daemon->logon()` 一個。每一個都把錯誤變成「安靜地回 0」，
於是最外層只看得到「撥號失敗」。

把最裡面那個的回傳值印出來，一步就到真因。

**規則**（強化 §32）：追「沒發生」的事情時，把路徑上的 `catch(` **全部**
列出來，從**最內層**開始挖——外層的 catch 只會告訴你「有東西失敗了」，
真正的訊息在最裡面那一個。

## 50. 偵測表漏一個名字，症狀是「挑到錯的東西」而不是「找不到」

`chongshengdeshijie` 從 noboot 走到 playable，最後兩步都是**偵測表少一個名字**：
玩家物件叫 `PPL_OB`（表裡只有 USER_OB／PLAYER_OB）、
起始房間叫 `STARTROOM`（表裡只有 START_ROOM）。

兩次的症狀都不是「偵測失敗」——是**退路生效**：
前者走 `new(ob->query("body"))` 拿到 0，後者落到 `VOID_OB`。
而報告寫的是「起始房間＝/obj/etc/void」，讀起來完全像正常結果。

**規則**：凡是「查不到就用退路」的偵測，退路生效時要**說出來**
（報告要能看出「這是退路，不是查到的」）。
加一個候選名字的成本是零，而漏一個的代價是一整台開不起來——
所以遇到新家族時，先把它的常數名補進表裡，不要先去修退路。

## 51. 「決定不收」要寫進清單旁邊，否則每輪重新發現一次

收錄清單用目錄名算（`comm -13 <(ls libs) <(ls 上游/libs)`），
所以「查過了、不收」這個結論如果只留在 commit 訊息裡，
下一輪它會重新出現在待辦清單上，然後再查一次、再得到同一個結論。

現在有 `libs/NOT_COLLECTED.md`。
**不要用空目錄佔位**——那會讓它被當成已收錄，連查都不會去查（§42）。

## 52. 閘門要掃**發佈出去的那份**，不是掃你手邊那份

`privacy-scan.sh` 每一輪都回報「✓ 沒有含密碼欄的存檔」，而映像裡躺著
**184 台、45,692 個真人玩家存檔**。腳本沒有寫錯——它用
`find libs -name '*.o'` 掃磁碟，而 `libs/<slug>/` 底下只有三個檔，
內容全部打包在 `mudlib.data` 這個 blob 裡。**磁碟上根本沒有 `.o`。**

這是本專案核心原則的另一種形狀：不是「閘門比產品寬鬆」，
而是「**閘門根本沒看產品**」——而它會一路顯示綠燈。

**規則**：任何打包／壓縮／編譯之後才發佈的東西，閘門必須掃**產物**。
判準要與修正**同源**（同一份規則、最好同一段程式碼），
否則「修的」和「驗的」會慢慢分岔，而分岔的那一段就是下一個缺口。

順帶：匯入端照**路徑**過濾（`data/**/*.o`）也漏了大半——
真實的存檔住在 `temp/login/`、`dump/2020-6-15/login/`、`drop/login_bak/`、
`suicide/login/`、`u/<巫師>/badplayer/login/`。
**照路徑猜資料性質，遲早會漏**；能看內容就看內容。

## 53. 上游說「差異只有排版」，那也要自己量一次

`hy2` 在收藏裡標著 noboot 好幾輪，理由是上游的 `wasm_status`。
而上游同時寫著它與 `huoying`（可玩）「567/569 個檔相同，差異只有排版」。
**這兩句話擺在一起就是矛盾**：同一份程式碼不會一台能跑一台不能。

量了才知道差別是一個數字：`MUD_PORT` 8000 vs config 的 40123。

**規則**：接受一個「不可能修好」的結論之前，先看它的**理由本身**
有沒有跟別的已知事實打架。這一台的線索一直擺在上游自己的說明裡。

## 54. 新規則的第一版通常太貪心——先量它會動到誰

`fixMudPortMismatch` 第一版取 switch 的第一個 case，
結果 **5 台現役可玩的台**被判定要把埠改成 http 埠（4015）或錯誤碼（1）。
如果直接套用，會用一條「修好一台」的規則弄壞五台。

收緊之後（現值命中任何 case 就不動、只挑名字像玩家埠的常數），
影響面從 5 台降到 **0 台**，而它要修的那一台照樣修好。

**規則**（承 §37）：新規則寫完先**乾跑全量**，把它會動到的每一台列出來。
名單不是零就逐個看——「它應該只會影響有問題的台」是假設，不是事實。

## 55. 指名一個打錯的路徑，健檢回報「1/1 通過」

`doctor` 的 `check()` 對「沒有 `mudlib.json` 的目錄」回空清單——
那是給「這不是映像目錄」用的。於是在 `tools/` 底下打
`doctor --fix libs/<slug>`（正確是 `../libs/<slug>`）會印出

```
1/1 台通過健檢
```

而那台其實 `.gz` 與 `.data` 不同步——全量健檢隨後就抓到了。
我當下相信了那個綠燈，還往下做了兩步。

**規則**：**指名**的目標找不到，一律報錯，不可以沉默地當成通過。
「沒有東西可檢查」和「檢查通過」是兩種結果，
共用一條輸出路徑就會製造假綠燈（§10：假綠燈比紅燈危險，
§21 表格裡「刻意跳過與靜默失效共用同一條路徑」是同一個形狀）。

## 56. 判準與動作要用同一個選擇器，否則偏差會等到某一台才發作

`verify-clicks` 用一組選擇器**找**標題列按鈕，卻用另一組去**點**它。
按鈕有三顆時兩者剛好對得上，只有一顆時就點到別的節點——
於是一台完全正常的 mudlib 被穩定判成「按下去沒有送出指令」。

而**不能用哪個選擇器的理由，就寫在同一支檔案上面幾行**。
我當時只把那個推理套用在偵測那一側。

**規則**：閘門裡「找到目標」與「對目標動作」共用一份選擇器／一份查詢。
兩份會在資料形狀改變時才發散，而那時它已經看起來正常運作很久了——
這與 §20（改登記表前先讀它的註解）同源：**答案就在旁邊，只是沒去讀**。

## 57. 本機全綠而線上停更時，先把「我看不到什麼」講清楚

線上索引落後 10 個 commit，而 privacy-scan／doctor／check-badges
在**全新 worktree（CI 條件）**上全部 exit 0，workflow 檔也沒被改過。
CI 日誌讀不到——repo 轉 private 之後 Actions API 回 404。

這時最沒有用的事情就是繼續猜（我一度往「repo 太大」的方向想，
但 `actions/checkout` 預設是淺 clone，那條推論站不住）。

**規則**：能在本機複製的 CI 條件全部複製過、而且全綠之後，
剩下的差異就在**看不到的那一段**。這時要做的是：
① 把已排除的清單寫下來（別人才不用重跑一次）
② 明確說「我需要 Actions 的失敗訊息」
而不是產生一堆無法驗證的假設。§1 的另一面——
**「已上線」要有證據，「為什麼沒上線」同樣要有證據。**

## 58. 線上停更的真因是**我自己推得太密**，不是有東西壞掉

`pages.yml` 用 `concurrency: { group: pages, cancel-in-progress: true }`，
而整條管線要跑完（209 個測試 → 掃描 → 建站複製 1.4 GB → 四個瀏覽器閘門 →
上傳 1.4 GB artifact → deploy）遠超過我的推送間隔。

證據是時間軸自己說的：**唯一部署成功的那次，前面有 119 分鐘沒有推送**；
之後每次間隔 10–34 分鐘，於是每個 run 都在跑完之前被下一次 push 取消。
本輪因為進入規劃階段停推了四十幾分鐘，站台**自己追上來了**
（連結 207 → 210，線上分級與本機一致）——沒有任何東西壞掉。

**規則**：診斷「線上沒更新」時，先看**自己的推送節奏**與管線長度的關係。
`cancel-in-progress: true` 對 CI 檢查是好事（省資源），對**部署**是災難：
它讓「一直有新東西」變成「永遠不上線」。
這也是 §57 的續集——本機能複製的條件全綠時，差異往往不在程式碼裡，
而在**節奏**這種不會出現在 diff 裡的東西。

## 59. 省空間的動作，要連工具會不會把它長回來一起想

刪掉 `libs/*/mudlib.data`（5.1 GB 的未壓縮衍生檔）之前，先確認了三件事：
`bootMud` 優先讀 `.gz`（安全）、`doctor` 兩種都認（安全）、
而 `fix-image.mjs` 的 `loadImage()` **無條件讀 `.data`**（會壞）。

補完回退還不夠——`saveImage()` 原本也**無條件寫回** `.data`，
那等於每修一台就長回一份，省空間的動作會被工具默默抵銷。

**規則**：刪除衍生檔之前，把「誰讀它」與「誰寫它」都列出來。
只處理讀的那一半，空間會自己長回來，而且沒有人會發現。

## 60. 一個判準散在「擋、修、驗」三處，那不是三層防線

玩家存檔的判斷原本有三份：匯入時擋的、修映像時移除的、掃描時驗的。
聽起來像三層防線，實際上是**三個會各自漂移的版本**——
而擋的那一份（最上游、最重要的那個）正是壞掉的那一份，
於是 45,692 個存檔一路通過另外兩層之前就已經進來了。

**規則**：同一個問題只能有一份判準。發現有第二份時，
第一件事不是「把它改成一樣」，是**把它刪掉改成引用**。
改成一樣只是把漂移的時間往後推。

驗證也要兩層：判準本身（含**應該放過**的反例）＋ 端到端（真的植入一筆，
三個入口都要出聲）。只驗前者會漏掉「接線接錯」，只驗後者會漏掉「剛好都錯得一樣」。

## 61. 抽成共用判準之後，呼叫端不可以再加「便宜的前置過濾」

三處判準合一的下一分鐘，`apply-fixup --dry-run --all` 就報出矛盾：
`stripPlayerSaves` 說 esI 有存檔，而 `scan-images` 說全部乾淨。
兩者共用同一個 `isPlayerSave()`——矛盾出在呼叫端多寫的一行：

```js
if (!f.path.endsWith('.o')) continue;   // 區分大小寫
```

esI 的存檔是 `ABEL.O`（大寫），194 筆因此在進到判準之前就被跳過。

**規則**：判準抽成共用之後，呼叫端**只准呼叫它**。
「先便宜地過濾一下」看起來無害，實際上把判準又拆成兩份，
而且拆出來的那一半**永遠不會被判準自己的測試覆蓋到**——
所以它可以錯很久而測試全綠。

順帶：這也是 §54 那支乾跑工具的第一份回報。
把「影響面用數字說」做成工具的價值，不只是看新規則，
還包括**讓兩個本來各說各話的地方在同一張表上對質**。

## 62. 「這條規則會觸發」與「這台需要它」是兩件事

規則覆蓋圖跑出來：`fixValidReadOwnData` 對 **194 台**會觸發。
第一直覺是「那就全部補上」——而那 194 台**全部是 playable**。
這條規則是為了修 `hy` 的一個具體症狀寫的（valid_read 擋住 mudlib 讀自己的
天氣表 → daemon 建不起來 → `look` 失敗），同樣的寫法在別台身上什麼事都沒有。

**規則**：覆蓋圖回答的是「誰還沒套過」，不是「誰需要」。
要從前者走到後者，中間**必須有一個症狀**。
沒有症狀就補，是把樣式當成範圍（§34），而且代價是實的：
一次改幾百台映像，`.git` 一次長好幾 GB。

這張圖的用法是**反向查詢**：某台出問題時，看它缺哪幾條，
挑最像的那一條，**只重建那一台**，量結果。
